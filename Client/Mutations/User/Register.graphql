mutation RegisterUser($newUser: RegisterUserInput!){
    registerUser(input: $newUser) {
        string
        errors {
            ... on EmailAlreadyInUseError {
                message
            }
        }
    }
}