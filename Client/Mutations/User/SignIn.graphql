mutation SignInUser($newUser: LogInUserInput!){
    logInUser(input: $newUser) {
        string
        errors {
            ... on InvalidEmailOrPasswordError {
                message
            }
        }
    }
}
